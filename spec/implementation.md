1.Authentication 
  -email+password/google autheitcation done 
  -resend emai service

2. image generation
   create image(img stored on r2storage)
   get image by id
   get all images
   delete image by id
   download image

3. credits
   get user current credits
   checkout/pricing plans

4. modles route
   get all models
   -- imp still not figured out howcredits will be costed ans generated based on model

5. Razorpay payment integration
  -create-order
--verify-payment
 -webhook handler